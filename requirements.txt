# Core
transformers>=5.0
tokenizers>=0.20
datasets>=2.18
huggingface_hub>=0.24
accelerate>=1.1.0
safetensors>=0.4
Pillow>=10.0
numpy>=1.23
pandas>=2.0
timm>=1.0          # for the iBOT vision encoder. NOTE: timm pulls torchvision.
                   # torch+torchvision are installed BEFORE this file from the cu126
                   # index (see slurm/train.slurm). Do NOT add torch/torchvision here
                   # or let timm pull a PyPI torchvision -- ABI mismatch with cu126
                   # torch breaks torchvision::nms and all transformers imports.
tqdm>=4.66

# torch is installed separately with the right CUDA wheels (see README / SLURM),
# because the correct build depends on the cluster's CUDA version. On Snellius:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# On Colab torch is preinstalled.

# Optional
wandb>=0.16          # only if training/eval with --wandb
